# 模型恢复与改进实施方案

## 目标与边界
保持跨被试评估、19通道顺序和坐标不变。不加载被否决的AAMP，不访问测试集，不覆盖已有训练产物。当前三折B1平均BA=0.5411，仅作为开发参考。不能保证准确率提升。已有大量未提交改动，本轮只增量修改，不回退。

## 分阶段实施
1. 可学习性诊断：从训练集固定抽取每类16个窗口；关闭dropout、增强、AMP和权重衰减，随机初始化B1。每隔若干更新在同一小样本上eval，记录CE、accuracy、每类召回、概率标准差、梯度范数和学习率。默认最多300步，达到98%且CE<0.1才判为通过；失败并不证明模型不可学习，应再检查学习率与预算。绝不作为泛化成绩。
2. 时间表示对照：保留默认avg路径完全不变。添加可选逐通道共享Conv1d+GELU前端，在平均降采样前提取局部非线性特征；不混合左右耳或更换通道坐标。可配置kernel=5，默认关闭。先测试输出、梯度、旧权重严格加载兼容性。
3. 固定三折比较：原B1 pool4、B1 pool2、卷积前端pool2。先seed42、相同25epoch预算和highest数值精度、训练折归一化。每次只改变一个因素；卷积必须与pool2对比，不能把两个变化的收益混在一起。输出逐被试BA/F1/AUC、两类召回、同checkpoint train/val诊断。无效分数/非有限loss直接失败，不能静默填0。
4. 简单模型对照：通道log variance、左右统计差异、正则化协方差+线性分类；标准化仅fit训练折。用于判断信号表示，不用测试集调参。
5. 候选晋级：开发三折平均BA提升至少0.01作为工程筛选标准（非显著性结论），至少两折提高且不出现类别坍缩，再扩展42/43/44种子。按被试/试次分组评估不确定性，同时保留窗口级主指标；不能把窗口当独立样本。
6. 教师稳定达到开发BA>=0.60并优于同协议CNN后，才开始蒸馏。已有测试集属于exposed holdout，最终独立评估最好新增被试。

## 实施文件与顺序
- scripts/diagnose_learnability.py：开发训练集小样本诊断，输出独立JSON，不生成正式模型结论。
- config.py、mini_neuript.py、scripts/train.py：可选时间前端参数及共享构建入口。
- tests：前端形状、反向、旧路径兼容测试。
- 后续独立补齐训练/搜索数值一致性、eval指标和三折实验驱动，未经测试不宣称完成。

## 执行控制
先检查现有Python/PyTorch环境与GPU；不安装到C盘，不自动开大规模搜索。短测试通过再训练。每轮输出放D盘，历史结果不删除。默认不启动云端付费实例。若本地缺少训练依赖或GPU，明确记录阻塞及可执行命令，不能把代码实现当作性能验证。
